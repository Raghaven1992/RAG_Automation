services:
  chatbot:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - PDF_DATA_PATH=/app/data
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
